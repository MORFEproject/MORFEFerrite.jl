[deps]
JuliaFormatter = "98e50ef6-434e-11e9-1051-2b60c6c9e899"

[compat]
# Pinned exactly: any version bump can change formatting output, which would turn
# the CI format check into spurious failures on files nobody touched. Bump this
# deliberately, together with a reformat commit.
JuliaFormatter = "= 2.12.4"
julia = "1.10"
